# Workspace instructions

Read and follow `AGENTS.md`. The canonical layout is product-first:
`products/plan`, `products/dwg`, `platform`, and `shared`.

Do not recreate legacy root `frontend`, `backend`, `resources`, or `agents`
directories. Run `npm run test:architecture` after any ownership/path change.
