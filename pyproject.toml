[project]
name = "plan"
version = "0.1.0"
description = "Mass-conditioned program-aware floor plan research scaffold"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["backend/tests"]
